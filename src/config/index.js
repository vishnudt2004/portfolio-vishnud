import { UI } from "./ui";
import {
  LAYOUTS,
  SECTIONS,
  ENABLED_SECTIONS,
  FOOTER_QUICK_LINKS,
  LAYOUT_IDS,
  SECTION_IDS,
} from "./sections";

const config = {
  UI,
  LAYOUTS,
  SECTIONS,
  ENABLED_SECTIONS,
  FOOTER_QUICK_LINKS,
  LAYOUT_IDS,
  SECTION_IDS,
};

const derivedConfig = {};

export default config;
export { derivedConfig };
