import { UI } from "./ui";
import {
  LAYOUTS,
  SECTIONS,
  ACTIVE_SECTIONS,
  IDS_MAP,
} from "./structure";

const config = {
  UI,
  LAYOUTS,
  SECTIONS,
  ACTIVE_SECTIONS,
  IDS_MAP,
};

export default config;
